#!/usr/bin/env bash
# This script pings an IP address 5 times and outputs the results.

# Check if an IP address is provided
if [ $# -eq 0 ]; then
    echo "Usage: $0 {IP_ADDRESS}"
    exit 1
fi

# Ping the provided IP address 5 times
ping -c 5 "$1"
