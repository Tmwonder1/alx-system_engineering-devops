#!/usr/bin/env bash

# This script displays a list of all currently running processes for all users,
# including those without a TTY, in a user-oriented format with process hierarchy.

# Using 'ps' command to list all processes with a user-oriented hierarchical view.
# -e: shows all processes
# -H: shows process hierarchy
# -f: shows full format listing, more user-friendly
ps -eHf
